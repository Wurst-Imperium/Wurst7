# What these scripts do

## `premerge.cmd`

To download a pull request and try it out locally before merging.

## `update_version_constants.py`

Used by the `auto_snapshot_update.yml` workflow to update various version constants.
